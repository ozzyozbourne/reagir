{
    "name": "reagir",
    "private": true,
    "type": "module",
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "build": "turbo run build",
        "dev": "turbo run dev",
        "test": "turbo run test"
    },
    "devDependencies": {
        "@types/bun": "latest",
        "turbo": "^2.7.3"
    },
    "peerDependencies": {
        "typescript": "^5"
    }
}
